{
  // Paths to scan with `fd`. You need to restart Sublime if changes are made
  // in "paths". Entering paths that contain many files will take longer to
  // scan but will not affect Sublime's startup time. E.g.:
  // "paths": ["~/Dev", "~/.config"],
  "paths": [],
}
